{
  "name": "memory-memu",
  "version": "0.3.0",
  "description": "OpenClaw memory plugin using memU (Anthropic LLM + Gemini embeddings)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "openclaw": "^2026.3.8",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "openclaw": "*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
